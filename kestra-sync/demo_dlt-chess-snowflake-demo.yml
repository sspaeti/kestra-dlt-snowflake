id: dlt-chess-snowflake-demo
namespace: demo

tasks:
  - id: sync_files
    type: io.kestra.plugin.git.SyncNamespaceFiles
    disabled: true # already synced files
    username: sspaeti
    password: "{{ secret('GITHUB_ACCESS_TOKEN') }}"
    url: https://github.com/sspaeti/kestra-dlt-snowflake/
    branch: main
    namespace: "{{ flow.namespace }}"
    gitDirectory: _files
    dryRun: true

  - id: python
    type: io.kestra.plugin.scripts.python.Commands
    namespaceFiles:
      enabled: true
    taskRunner:
      type: io.kestra.plugin.core.runner.Process    
    env:
      BIGQUERY_PROJECT_ID: "{{ secret('DESTINATION__SNOWFLAKE_CREDENTIALS') }}"
      DESTINATION__SNOWFLAKE_PASSWORT: "{{ secret('DESTINATION__SNOWFLAKE_PASSWORT') }}"
      DESTINATION__SNOWFLAKE_HOST: "{{ secret('DESTINATION__SNOWFLAKE_HOST') }}"
    beforeCommands:
      - pip install dlt[snowflake]
      - ls dlt
      - pwd
    # script: |
    #   print("hello")
    commands:
      - python dlt/dlt-chess-snowflake.py


